# Bootstrap TODO — `pdfreactor-modules`

Generated by `build-config/scripts/bootstrap-module-repo.py` on
the monorepo. Review and resolve everything below before pushing
this directory to `ericf-ro/pdfreactor-modules`.

## Required review

**The following carry-over could not be automated:**

- [ ] Custom tasks (e.g. generateCssParser, obfuscate) — copy by hand.
- [ ] References to rootProject.projectDir won't work standalone — either inline the path or restructure.
  Source-of-truth: monorepo's `gradle-modules/pdfreactor-modules/build.gradle`.

## Verify the build before pushing

1. Set `CODEARTIFACT_AUTH_TOKEN`:
   ```
   aws --profile awsdev sso login   # if not already logged in
   export CODEARTIFACT_AUTH_TOKEN=$(aws --profile awsdev codeartifact \
       get-authorization-token --domain realobjects \
       --domain-owner 811783768095 --query authorizationToken --output text)
   ```
2. Build:
   ```
   ./gradlew assemble       # via WSL on Windows
   ```
3. (Optional) test publish to a local Maven cache to inspect the POM:
   ```
   ./gradlew publishToMavenLocal
   ls ~/.m2/repository/com/realobjects/pdfreactor-modules/*/
   ```

## Push to GitHub

1. Create empty repo `ericf-ro/pdfreactor-modules` on GitHub (private).
2. From this directory:
   ```
   git init -b main
   git add -A
   git commit -m 'Initial import from monorepo'
   git remote add origin https://github.com/ericf-ro/pdfreactor-modules.git
   git push -u origin main
   ```
3. The first CI run should publish a `1.0-SNAPSHOT` to CodeArtifact.
   Verify:
   ```
   aws codeartifact list-package-versions --domain realobjects \
     --repository snapshots --region eu-central-1 \
     --format maven --namespace com.realobjects --package pdfreactor-modules
   ```

## Then in the monorepo

1. Swap every `project(':gradle-modules:pdfreactor-modules')` ref for
   `'com.realobjects:pdfreactor-modules:1.0-SNAPSHOT'`.
2. Add `pdfreactor-modules.version=1.0` to
   `build-config/ivy/ivysettings.properties` if needed.
3. Run `./gradlew help` to confirm the build still parses.
4. `git rm -r gradle-modules/pdfreactor-modules`
5. Remove `gradle-modules:pdfreactor-modules` from `settings.gradle`.
6. Re-run `python build-config/scripts/generate-module-dependents.py`.
7. Commit and push.

## Provenance

- Source copied from: `gradle-modules/pdfreactor-modules/`
- Group ID: `com.realobjects`
- Artifact ID: `pdfreactor-modules`
- Version: `1.0-SNAPSHOT`
- Bootstrap script version: see `build-config/scripts/bootstrap-module-repo.py`
